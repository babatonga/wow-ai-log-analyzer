# --- Python ---
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info
.venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/
htmlcov/
.coverage
coverage.xml

# --- Env / secrets ---
# Never commit anything that may carry real values. .env.example is the
# tracked template; everything else with secrets stays local.
.env
.env.local
.env.*.local
secrets/
*.pem
*.key

# --- Node / Next.js ---
node_modules/
.next/
out/
.turbo/
*.tsbuildinfo
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# --- Docker ---
docker-compose.override.yml

# --- IDE ---
.idea/
.vscode/
*.swp

# --- OS ---
.DS_Store
Thumbs.db

# --- Project ---
# Anchored to root — earlier the bare ``data/`` matched ANY directory
# called "data" anywhere in the tree, including ``backend/app/data/``
# (which contains seed JSON + email templates that MUST ship with the
# backend image). Leading slash anchors the pattern at repo root.
/postgres-data/
/redis-data/
/uploads/
# Iteration scratch space for AI-generated brand/illustration assets — only
# the curated outputs land in frontend/public/brand/ which is committed.
frontend/public/generated/

# --- Editor / scratch / backups ---
*.bak
*.tmp
*.orig
*.rej
.DS_Store

# --- Claude Code workspace ---
.claude/projects/
.claude/

.local/